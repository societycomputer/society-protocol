{
  "name": "society-computer-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx serve . -p 3000",
    "build": "echo 'Static site — deploy the directory as-is'",
    "preview": "npx serve . -p 3000"
  }
}
